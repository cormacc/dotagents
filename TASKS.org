#+TITLE: dotagents tasks
#+AUTHOR: Cormac Cannon
#+LANGUAGE: en
#+DATE: 2026-05-07
#+LINK: task file:TASKS.org::#%s
#+LINK: archive file:TASKS.archive.org::#%s
#+SETUPFILE: ./TASKS.local.org
#+SETUPFILE: ./TASKS.setup.org
#+ARCHIVE: TASKS.archive.org::* From %s


* Table of Contents                                       :TOC_1_gh:noexport:
- [[#improvements][Improvements]]
- [[#fixes][Fixes]]
- [[#housekeeping][Housekeeping]]
- [[#agent-feedback][Agent feedback]]

* Improvements


** TODO [#C] Improve terminal extension :pi:
:PROPERTIES:
:CUSTOM_ID: 984677a0-7d87-4ade-bacb-4bf7fe7796af
:END:
The terminal extension is only half cooked. We refactored a while ago to use tmux across all backends for pane management and do minimal pane creation/deletion using sway, kitty etc. This was a good choice, but the key bindings / workflows should be reviewed and updated - they hark back to an earlier design (or lack thereof).
- Evaluate whether we should use zellij rather than tmux as the terminal multiplexer.
- Redesign the slash commands and keybindings around the multiplexer workflow / semantics.
** TODO [#D] Investigate Allium :org:
:PROPERTIES:
:CUSTOM_ID: a46d6a4d-e72f-4923-8472-2daf28cb29d0
:END:
May be a useful complement to tasks / memory system?
- https://juxt.github.io/allium/
- https://plugins.eca.dev/plugins/allium/
** TODO [#D] Check out rpiv-todo overlay for ideas :org:
:PROPERTIES:
:CUSTOM_ID: 60e8eff5-26a9-4d4b-8c1a-3c70321beccd
:END:
:LOGBOOK:
- State "CANCELLED"      from "TODO"      [2026-05-08 Fri 07:41]
- State "TODO"      from "CANCELLED"      [2026-05-08 Fri 07:41]
:END:
https://pi.dev/packages/@juicesharp/rpiv-todo Haven't checked it out yet, but might be a nicer overlay, with a different underlying task store.
** TODO [#D] Check out rpiv-btw :pi:
:PROPERTIES:
:CUSTOM_ID: ec8e6eb3-dcfc-4124-ad1c-17e88d8562b6
:END:
https://pi.dev/packages/@juicesharp/rpiv-btw Ask a one-off question without polluting context... sounds useful
** TODO [#D] Evaluate PiArgus as a replacement for search/browser tools :pi:
:PROPERTIES:
:CUSTOM_ID: c354bc27-af23-494c-a1ce-7abfa832d7cf
:END:
This has some nice integration with PiDocs, and uses searx as a search backend...
- https://github.com/GeneGulanesJr/PiArgus
- https://github.com/searxng/searxng
** TODO [#C] Evaluate PiLapis as complement to org-memory :org:
:PROPERTIES:
:CUSTOM_ID: df50e088-86c0-44be-8d0a-3a09fce88d2b
:END:
This extension looks interesting -- I'm enjoying the org-memory flow for task tracking and planning,
but maybe there's some potential synergy here... a lower layer more immediately useful to the agent...
https://github.com/GeneGulanesJr/LaPis
** TODO [#D] Review self-improvement skill w.r.t. org-memory protocol :org:
:PROPERTIES:
:CUSTOM_ID: 64a816ea-7484-4b0f-b8ed-aabbb5c66e52
:CREATED: [2026-05-02 Sat 12:22]
:END:
:LOGBOOK:
- Created [2026-05-02 Sat 12:22]
:END:
Spawned from the dev-agent-backlog prior-art review (parent task 5aed17ed-cfc4-4ae3-bb24-57c7d974a142). The review deferred dev-agent-backlog's "solution / lesson capture" idea on the basis that it overlaps with the existing self-improvement skill and is a separate concern from the task lifecycle protocol.

Re-examine the self-improvement skill against the org-memory protocol (org-tasks, org-plan, org-jira) and decide:
- whether durable lessons / solutions should land as their own org artefacts, separate change-records, or knowledge-base files
- whether the skill should integrate with TASKS.org via spawned TODOs vs remain orthogonal
- whether anything in the dev-agent-backlog HTML summary (design/log/2026-05-01-dev-agent-backlog-review-summary.html) should be borrowed for the self-improvement skill specifically
** TODO [#D] Extract org-mode tooling to a dedicated pi extension with a tree-sitter foundation :pi:extension:org:refactor:
:PROPERTIES:
:CUSTOM_ID: f361c429-45dd-4364-9fa3-1f77bd7c600a
:CREATED: [2026-05-12 Tue 10:18]
:BLOCKED-BY: 1b496799-6198-46ca-8084-7a1fbed6f250
:END:
:LOGBOOK:
- Created [2026-05-12 Tue 10:18]
:END:
Once `org_read_section` ships in-place inside `pi/extensions/tasks/`, promote org-file primitives out into their own `pi/extensions/org/` extension so non-task consumers (jira, emacsclient, future doc tooling) can depend on org parsing without pulling in the tasks UI.

Motivation:
- The section-reader primitive (TASKS.org task 1b496799 / change-record `design/log/2026-05-12-tasks-read-section.org`) is registered under the `org_*` tool namespace specifically to anticipate this extraction — the contract stays stable across the move.
- `nvim-orgmode/tree-sitter-org` is an actively maintained tree-sitter grammar (MIT, weekly-touched, backs nvim-orgmode) whose `(section)` / `(headline)` / `(body)` node shapes map cleanly onto the slicing and parsing tasks we hand-roll today in `pi/extensions/tasks/parser.ts` and `section.ts`.
- Pi today has no JS-side tree-sitter runtime; introducing one for a single ~50-line primitive was rejected as disproportionate. Doing it once, as part of an extension extraction that amortises across multiple consumers, is the right time.

Acceptance criteria:
- New extension at `pi/extensions/org/` with its own `package.json`, `index.ts`, `README.md`.
- `web-tree-sitter` (or native bindings, decided during the work) wired up with `tree-sitter-org.wasm` bundled or fetched at install time; async grammar init handled lazily so cold-start cost only hits the first call.
- `org_read_section` migrated from `pi/extensions/tasks/` to the new extension and reimplemented using a tree-sitter query against `(document (section (headline (item) @name)) @section)`. Tool contract (name, parameters, return shape) unchanged so existing callers keep working.
- Decision documented in the new extension's README on whether to migrate `parser.ts`'s heading / property-drawer regexes to tree-sitter queries in the same change, or leave that as a follow-up.
- Closure-time `evaluateSummaryRefresh` path in the tasks extension continues to work synchronously — either by keeping a thin regex-based `hasSummaryHeading` check in `summary.ts`, or by ensuring the tree-sitter init is eager enough that the sync read remains viable. Recorded as a Decision.
- Unit tests for the migrated primitive cover the same five acceptance-criteria cases as the original (section present, absent, last, no headings, literal `* ` inside src block).
- README cross-references the `org-tasks` resume read order and the future `tasks_resume_brief` consumer.

Blocked by the in-place `org_read_section` primitive shipping first (TASKS.org task 1b496799) — no point doing both at once.
** TODO [#C] pi tasks/jira: walk the #+SETUPFILE chain when loading config :pi:org:tasks:jira:followup:
:PROPERTIES:
:CUSTOM_ID: 17e2a3e4-09c0-4068-bb3a-371382fcff78
:CREATED: [2026-05-14 Thu 14:33]
:END:
:LOGBOOK:
- Created [2026-05-14 Thu 14:33]
:END:
Follow-up to 2026-05-14-jira-config-setupfile: the pi-side `loadJiraConfig` and `readEffectiveOrgContent` currently follow only one `#+SETUPFILE:` level and reapply local-over-shared precedence in code. Org-mode itself merges multiple `#+SETUPFILE:` declarations with first-declared-wins semantics, so the new local-first chain (`#+SETUPFILE: ./TASKS.local.org` before `#+SETUPFILE: ./TASKS.setup.org`) gives the correct override behaviour natively. Extend pi to walk the chain (with a depth/cycle guard) and drop the bespoke precedence layer so pi and Emacs converge.

Acceptance:
- `readEffectiveOrgContent` (or its successor) accepts any number of `#+SETUPFILE:` declarations in declaration order, with a small cycle/depth guard.
- `loadJiraConfig` / equivalent task-extension lookups delegate precedence to the org-mode merge order rather than reapplying local-over-shared in code.
- Unit coverage for chained setupfiles and for the cycle guard.
** TODO [#D] Implement vulpea tasks UI for emacs :org:emacs:ui:
:PROPERTIES:
:CUSTOM_ID: 92d3a8d9-a01f-4f93-bc3a-90f24d12a4e5
:CREATED: [2026-05-15 Fri 09:53]
:END:
:LOGBOOK:
- Created [2026-05-15 Fri 09:53]
:END:
Follow-on from [[plan:2026-05-14-evaluate-vulpea-org-memory.org][vulpea evaluation]]. Prototype an Emacs-local tasks side panel only after deciding the minimum ID mirror/custom extractor bridge needed to make org-memory tasks first-class vulpea notes.
* Fixes

** TODO Local tasks duplicated in overlay when selected? :bug:pi:
:PROPERTIES:
:CUSTOM_ID: 0c48e3b3-6a32-4158-9d4c-59c17c073abe
:END:
When I do the following....
- Create an empty TASKS.org
- Create a new task in TASKS.local.org
- In the pi tasks extension, select the new task
The new task is initially duplicated to TASKS.org, then deleted from TASKS.local.org It's possible that I accidentally hit promote, but don't believe so. Investigate -- reproduce manually, then follow up as appropriate.
* Housekeeping

** STARTED Create self-improvement skill :pi:
:PROPERTIES:
:CUSTOM_ID: 2262ed25-f0fc-40fd-9d21-41a0b16fb3a1
:CREATED: [2026-04-29 Wed 00:55]
:STARTED: [2026-04-29 Wed 08:00]
:END:
#+IMPORT: [[plan:2026-04-28-create-self-improvement-skill.org]]
* Agent feedback

** TODO [#D] Evaluate pi-lsp over home-rolled lsp :pi:
:PROPERTIES:
:CUSTOM_ID: 0ed56048-9f53-44b4-8a5a-2dbc858e6f04
:END:
https://pi.dev/packages/@dreki-gg/pi-lsp
