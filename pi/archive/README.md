# Pi archive

This tree is inactive historical source. Pi does not auto-discover extensions or skills under `pi/archive/`, and neither the pi package manifest nor the Nix package slice includes it.

The archived leader-menu, tty, webdriver, web-search, and related resources are retained for migration rationale and prior-art reference. Do not treat them as installed replacements or delete them solely because an active package now provides similar behavior.
