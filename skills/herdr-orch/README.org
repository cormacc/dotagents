#+TITLE: herdr-orch -- design rationale and maintenance notes

Supplements [[file:SKILL.md][SKILL.md]] with the rationale and maintenance context for the =oh= CLI (renamed from =subagent=; =bb run subagent= survives as the Babashka task alias). Ordinary delegation should not load this file.

Division of labour:

| Document | Canonical content |
|---+---|
| [[file:SKILL.md][SKILL.md]] | Agent-runtime delegation, trust, and pane-safety policy |
| [[file:scripts/README.md][scripts/README.md]] | CLI invocation, tests, and smoke entry points |
| [[file:scripts/docs/contract.md][scripts/docs/contract.md]] | Every mechanical contract: CLI, JSON, environment, ledger, envelope, model resolution, retro/spawn gating, placement, fan-in, pruning, progress, parent push, artifact links, labels, and geometry |
| This file | Rationale, architecture, review notes, and history |

* Why this skill exists

Herdr supplies panes, tabs, and agent lifecycle operations, but not a delegation protocol: persona selection, naming, parent/child identity, durable completion, or safe pane disposal. Those mistakes are costly. A parent can mistake transcript text for completion or destroy work by closing a pane before capturing its result.

The skill therefore separates policy from mechanics. [[file:SKILL.md][SKILL.md]] carries the small runtime policy an agent needs, while the Babashka CLI makes identity, publication, validation, and ownership checks repeatable. The upstream [[https://github.com/ogulcancelik/herdr/blob/master/SKILL.md][Herdr skill]] remains the authority for direct pane and agent operations; this repository vendors a copy at [[file:../herdr/SKILL.md][skills/herdr/SKILL.md]], but the CLI path needs only the =herdr= binary and =HERDR_ENV=1=, so standalone installs of this skill work without it.

* Design rationale

** Harness-neutral roster and CLI

The packaged roster under [[file:subagents/][subagents/]] uses Markdown plus frontmatter. =name= and =description= are advisory; =kind=, =model=, =spawns= (§ Per-persona spawn capability) and =retro= (§ Optional process retrospectives) are the keys the CLI actually reads. Persona definitions resolve project override (=<git-root>/.agents/subagents/=), then home override (=~/.agents/subagents/=), then this packaged directory; definitions shadow atomically by name. The resolved =kind= selects the child harness, so a parent is not restricted to its own harness. Keeping orchestration in a pi extension would have broken that property.

The separate =config.edn= chain has the same default ← home ← project order but a different merge rule: a home or project row replaces the full row with that model ID, including =heavy=, =middle=, =light=, or =feather=; it does not deep-merge or select a kind. Those aliases map only after kind selection; their per-kind values are enumerated once, in [[file:scripts/docs/contract.md][contract.md]] § Model resolution, which =default-config-content-contract= pins to =config.edn=.

The [[file:../../design/log/2026-07-24-herdr-subagent-skill-scripts.org][2026-07-24 change-record]] records the implementation choice: Babashka behind a thin launcher was preferred to a harness-specific extension or pure Bash. It provides vector argv, JSON handling, testable protocol logic, and atomic filesystem operations without adding Maven dependencies. Folding delegation into =ot= was rejected because task memory and terminal delegation are separate domains.

** Why mechanics belong in a CLI

The earlier prose-only protocol required the parent to perform label calculation, geometry selection, split/start/prompt operations, bounded waiting, envelope parsing, artifact checks, and pane closure by hand. Besides repeated model round-trips, partial compliance failed silently: identities could be mistyped, visible summaries substituted for result files, and panes closed before capture.

The CLI absorbs those operations behind one spawn/collect interface and returns the validated envelope inline. Capability probes add subprocess work before mutation, but convert unsupported-Herdr states into an actionable error before a pane or ledger assignment is partially created. Exact invocation counts are intentionally not frozen here because lifecycle hooks change; use the fake-Herdr log when evaluating current overhead.

** Optional process retrospectives

The [[file:../../design/log/2026-07-27-subagent-retrospectives.org][2026-07-27 change-record]] added child-side process learning without giving children persistence authority. A gated child scans context it already holds and can attach compact =signal → category → proposed rule= candidates to the durable result. The parent still verifies, deduplicates, routes, and seeks approval, so self-retro remains testimony rather than an automated write path.

=PROCESS:= was added as an optional trailing v1 annotation instead of a separate retro artifact or envelope v2. The position preserves existing v1 readers, omission preserves old output when there are no candidates, and the ledger already persists validated envelopes. =:child-session= complements this with a best-effort transcript reference for thin candidates or crash investigation. The exact grammar and degradation rules live only in [[file:scripts/docs/contract.md][contract.md]].

The =retro= skill is optional by design. Project/global skill resolution lets integrated installations enable it by default, while third-party installations without it continue delegating with no injected instruction. Only explicit =--retro= fails when the capability is absent because an operator request must not silently become a no-op. The live smoke follows the same rule and reports a skipped retro leg when the skill is unavailable.

** Where assignment state lives

Assignment state lives under =<assignment-root>/.tmp/herdr-orch/=, deliberately outside =.agents/=: that tree is durable agent configuration, and codex under =--sandbox workspace-write= mounts it read-only so a child cannot rewrite its own instructions. A =RESULT= path inside it is unwritable, which silently makes publication -- the protocol's only completion signal -- impossible for codex-kind children. The same reasoning applies to any scratch path handed to a child.

** Per-persona spawn capability

The [[file:../../design/log/2026-07-27-subagent-per-persona-spawn-capability-re.org][2026-07-27 spawn-capability change-record]] replaced the prose-only planner exception with declared roster capability: a frontmatter =spawns:= allow-list resolved once at spawn (flag > frontmatter > default deny), recorded on the ledger as =:spawns=/=:spawns-source=, injected as =HERDR_ORCH_SPAWNS=, and enforced mechanically by the child CLI before preflight, ledger allocation, or pane mutation. Prompt-only enforcement was rejected because its failure modes are billable and pane-creating; child self-resolution from the roster was rejected because it cannot carry a per-spawn flag override and mid-flight roster edits would change a live child's capability. Any below-root spawn injects an empty grandchild policy with source =depth= -- one nesting level absolutely, no depth counter -- so labels stay display-only metadata. Below-root one-at-a-time cardinality deliberately remains prompt-strength doctrine.

* Configuring a user override

=config.edn= tunes three things: how a model ID is translated for a harness (=:models=), how a harness is invoked (=:harnesses=), and what a spawn defaults to (=:defaults=). It never defines a persona -- those are =<name>.md= files with their own resolution chain -- and it never selects a kind.

The chain is packaged =skills/herdr-orch/subagents/config.edn= ← home =~/.agents/subagents/config.edn= ← project =<git-root>/.agents/subagents/config.edn=, project winning. In this repository the home layer is [[file:../../subagents/config.edn][subagents/config.edn]], symlinked to =~/.agents/subagents/= by the dotfiles =agents.nix=; edit it there rather than under =~=.

** The merge rule that catches people

=merge-config= is =merge-with merge=, so the merge is exactly two levels deep:

- Top-level keys (=:models=, =:harnesses=, =:defaults=) merge across files.
- One level down, an entry *replaces* its counterpart wholesale. An override =:models= row for ="heavy"= replaces the packaged ="heavy"= row rather than merging columns into it, and an override =:harnesses :claude= entry replaces the packaged one -- so it must restate =:model-flag=, or validation rejects the file by name.
- =:defaults= is the exception, because its values are scalars: each key merges independently.

** Worked example

A home override that pins the =heavy= tier to a specific Claude release, makes claude-kind children usable unattended, and puts every child in its own tab:

#+begin_src clojure
{:models   {"heavy" {:pi "anthropic/claude-fable-5"
                     :claude "claude-fable-5-20260714"   ; pinned, not the floating alias
                     :codex "gpt-5.6-sol"}}               ; restated: the row replaces, not merges
 :harnesses {:claude {:model-flag "--model"               ; mandatory even when unchanged
                      :extra-args ["--permission-mode" "auto"]}}
 :defaults  {:placement :tab}}
#+end_src

The =:codex= column and the =:pi= column are restated because the row replaces the packaged one; omitting them would leave =--model heavy= untranslated for those kinds, and an untranslated ID passes through to the harness verbatim. =:model-flag= is restated for the same reason at the =:harnesses= level, where omitting it is a hard validation error rather than a silent pass-through.

=:extra-args= exists so an override can relax a harness's interactive command approval; without it, claude and codex children stall at their first tool call in a pane nobody is watching. Prefer a bounded grant to a blanket bypass -- the live home override documents the reasoning and the rejected alternatives at length, and [[file:scripts/docs/contract.md][contract.md]] § Harness =:extra-args= gives the validation rules.

** Verifying an override

Config is parsed and shape-validated before any ledger allocation or pane mutation, and every failure names the offending file, so a broken override costs nothing billable. =:defaults= is a closed map -- =:placement= only, valued =:split=, =:tab=, or =:tab-split= -- so a typo fails loudly instead of being ignored.

Check what a spawn would actually do without spawning it:

#+begin_src sh
oh task run scout --task x --model heavy --print-prompt
#+end_src

The preview envelope reports =kind=, the canonical =model=, the translated =model-args= (the native flag and value the harness receives), plus the resolved =placement=, =retro=/=retro-source=, and =spawns=/=spawns-source=. If =model-args= still shows your alias rather than a harness-native ID, the row did not match the resolved kind.

* Architecture

| Namespace | Responsibility |
|---+---|
| [[file:scripts/src/herdr_orch/core.clj][core.clj]] | Pure protocol logic: roster/skill resolution, kind/model policy, retro and spawn policy, geometry, labels, and envelope render/parse/validation. |
| [[file:scripts/src/herdr_orch/herdr.clj][herdr.clj]] | Vector-argv Herdr adapter, JSON/error decoding, minimum-version check, and non-mutating capability preflight. |
| [[file:scripts/src/herdr_orch/ledger.clj][ledger.clj]] | Assignment-root resolution, atomic ledger writes, result-path allocation, and exclusive-create monotonic index allocation. |
| [[file:scripts/src/herdr_orch/cli.clj][cli.clj]] | Command orchestration, prompt composition, spawn-policy enforcement, publication, capture, session backfill, ownership-scoped closure, and bounded waiting. |
| [[file:scripts/src/herdr_orch/smoke.clj][smoke.clj]] | Explicitly guarded billable integration smoke; never loaded by =bb test=. |
| [[file:scripts/oh][oh]] | Bash launcher selecting repository =bb.edn= or the standalone =-Sdeps= path while preserving caller cwd. |

* Review and maintenance notes

** Known regression boundaries

The regression suite names the important boundaries; preserve the tests rather than duplicating their mechanics here:

- =bounded-poll-timeout-without-result= must fail if the wait-loop sleep is removed.
- =launcher-matrix-selects-branch-and-preserves-cwd= must fail if launcher path canonicalisation or caller cwd preservation regresses.
- result capture must precede ownership-scoped pane closure, and foreign/unresolvable parent identity must retain the pane.
- retro tests must remain behavioural at prompt/CLI boundaries: frontmatter values are strings, value-less flags participate in two argv consumers, and optional annotations must not demote a valid result.
- live-smoke helper assertions are unit-tested because the billable smoke is never part of the standard suite.

All =herdr= calls in =src/= must remain vector argv. Keep the standalone launcher dependency-free beyond Babashka built-ins. When Herdr changes, review =required-capabilities= and the fake fixture's help output together.

** Test isolation caveat

=ORCH_ASSIGNMENT_ROOT= relocates project config lookup and all assignment state into each fixture. The CLI tests symlink both the tracked config and project =skills/= tree into that root. They also set =$HOME=, but Babashka's =System/getProperty("user.home")= comes from the JVM/OS property rather than the subprocess environment, so =$HOME= alone cannot hide the global config or skill probes. Tests of the no-global-skill branch therefore belong at the pure =core= policy boundary unless the process property itself is controlled.

A full =bb test= should create no entries under the repository's live =.tmp/herdr-orch/= tree. When mutation-testing assignment-root or launcher behavior, set an override and inspect that tree afterwards.

** Reflexive changes

A child editing =scripts/= is modifying the same launcher used by its own =publish= and the parent's =collect=. Before delegating such work, copy a known-good scripts tree outside this repository and give the child its launcher as a fallback. Put the snapshot where the launcher's repository-root walk cannot rediscover this checkout's =bb.edn=; otherwise the supposed snapshot loads working-tree source.

Publication identity comes from injected =HERDR_ORCH_*= variables rather than the ledger, so an isolated snapshot can publish the assigned result. Verify the fallback before relying on it.

** Deployment and live smoke

=agents.nix= deploys =~/.agents/skills= as a directory symlink, which includes this skill's packaged =subagents/= directory. Launcher canonicalisation must therefore use physical paths when locating the repository configuration while retaining the absolute deployed launcher path injected into the child. Do not project packaged definitions into =~/.agents/subagents/=: that path is deliberately unmanaged and reserved for genuine home overrides; project overrides remain =<git-root>/.agents/subagents/=.

=bb smoke-subagent= is billable and guarded by =HERDR_ENV=1=, =ORCH_LIVE_SMOKE=1=, and =ORCH_LIVE_SMOKE_MODEL= (with optional =ORCH_LIVE_SMOKE_KIND= to exercise a non-default harness). It calls =cli/execute= in-process and therefore cannot validate the Bash launcher. Launcher changes need a separate deployed-path smoke from a scratch project. The retrospective leg deliberately lets the child encounter an unannounced invalid command: announcing the trap turns it into a known one-off that the =retro= eligibility gate correctly rejects.

Consequently a smoke run's =PROCESS= candidates are manufactured, not observed, and are indistinguishable from genuine ones in the collected envelope. Never route them to =self-improvement= or report them as durable findings.

* Known gaps

- The CLI implements one child per invocation for =run=/=start=; multi-child fan-out scheduling (how many in flight, when to replace one) and resident lifecycle remain parent policy in [[file:SKILL.md][SKILL.md]] -- no command spawns or schedules more than one child. Fan-in, however, now has a dedicated primitive: =collect --any= (see [[file:scripts/docs/contract.md][contract.md]] § Fan-in) captures whichever in-flight child of the caller's session publishes first, without scheduling anything itself.
- =HERDR_ORCH_BIN= intentionally carries the deployed absolute path rather than a canonical repository path; launcher-matrix coverage is its guard.
- =ot ready= cannot resolve blockers inside =#+IMPORT:=-linked plan files, so plan-internal dependencies report =[opaque]=. This is an org-tasks limitation, not a delegation issue.

* History

- [[file:../../design/log/2026-07-22-harness-agnostic-subagents.org][2026-07-22]] -- harness-neutral roster and kind/model resolution.
- [[file:../../design/log/2026-07-23-add-hierarchical-pane-labels-to-herdr-su.org][2026-07-23]] -- hierarchical pane labels.
- [[file:../../design/log/2026-07-24-herdr-subagent-skill-scripts.org][2026-07-24]] -- delegation CLI and implementation-review remediations.
- [[file:../../design/log/2026-07-27-subagent-retrospectives.org][2026-07-27]] -- optional child retrospectives, process annotations, and child-session recording.
- [[file:../../design/log/2026-07-27-subagent-per-persona-spawn-capability-re.org][2026-07-27]] -- per-persona spawn capability replacing the planner-only exception.
- [[file:../../design/log/2026-07-29-subagent-harness-agnostic-model-resoluti.org][2026-07-29]] -- harness-agnostic model resolution.
- [[file:../../design/log/2026-07-29-subagent-comms-housekeeping.org][2026-07-29]] -- parent/child communication housekeeping.
- [[file:../../design/log/2026-07-29-subagent-package-default-example-subagen.org][2026-07-29]] -- packaged default persona roster.
- [[file:../../design/log/2026-07-29-subagent-implement-the-advisor-strategy.org][2026-07-29]] -- advisor strategy (superseded by 2026-07-31).
- [[file:../../design/log/2026-07-30-subagents-make-default-spawn-options-con.org][2026-07-30]] -- configurable default spawn options.
- [[file:../../design/log/2026-07-31-subagents-review-advisor-strategy-defaul.org][2026-07-31]] -- advisor/tier benchmark rounds behind current tier guidance.
- [[file:../../design/log/2026-07-31-subagents-retire-the-mandatory-advisor-c.org][2026-07-31]] -- retired the mandatory advisor consult and the frontier-executor persona.
- [[file:../../design/log/2026-08-01-herdr-orch-absorb-pi-herdr-coverage-into.org][2026-08-01]] -- absorbed pi-herdr coverage; skill/CLI renamed to =herdr-orch=/=oh=.
- [[file:../../design/log/2026-08-03-subagent-make-claude-kind-persona-inject.org][2026-08-03]] -- claude-kind persona injection and bounded-permission overrides.
