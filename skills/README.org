#+TITLE: General-purpose agent skills

These skills are symlinked into =~/.agents/skills/= by the consuming
project's Home Manager module (or the Vercel Labs ~skills~ installer,
or any Agent Skills compatible loader). They are available to every
coding agent that honours that location (=pi=, =claude-code=, =codex=).
For pi-only skills that depend on specific extensions, see
[[file:../pi/skills/][../pi/skills/]].

The =org-tasks=, =org-plan=, and =org-jira= skills below are also
shipped as part of the =agent-org-memory= pi package alongside the
=tasks=, =jira=, and =emacsclient= extensions. See
[[file:../README.md][../README.md]] for the package boundary, install routes (npm/git/Nix),
and the editable-vs-packaged Home Manager modes.

* Skills

** First-party
I.e. self authored skills.

*** Mature
The following have gone through some cycles of refinement and are in regular use:
- [[file:org-jira/SKILL.md][org-jira]] :: Jira-specific authoring conventions on top of
  =org-tasks= — =:LINKED_ISSUES:= drawer key, =#+JIRA_*= keywords,
  agent prompts that drive the atlassian MCP.
- [[file:org-plan/SKILL.md][org-plan]] :: Drafting, reviewing, and executing implementation
  plans as change-record files linked from =TASKS.org=. Owns
  change-record section conventions, closure-time summary refresh,
  and TASKS.org subtask migration into plans.
- [[file:org-tasks/SKILL.md][org-tasks]] :: The =TASKS.org= task-memory protocol — TODO
  states, IDs, =#+IMPORT:= links, status discipline, archiving,
  and the resume read order that loads =* Summary= first.

*** Neglected / undernourished
The following are in-development, or in some cases just vague statements of intent:
- [[file:code-review/SKILL.md][code-review]] :: Very bare bones code-review skill. Needs development.
- [[file:self-improvement/SKILL.md][self-improvement]] :: Capture friction with agent / pi
  configuration (AGENTS.md, skills, prompts, extensions, project
  conventions) at the moment it arises and route it as actionable
  TODO work. Project-local fixes land in the current project's
  =TASKS.org=; global fixes are handed off to a session in this
  dotfiles repo via pi-intercom (auto-spawned if none is alive).

** 3rd-party
I.e. skills adopted/adapted from other sources (primarily via `npx skills find`).
In most cases there's been some modification / drift from the source.

- [[file:clojure/SKILL.md][clojure]] :: REPL-driven Clojure development for writing,
  editing, and debugging code. Triggers on =.clj=, =.cljs=, =.cljc=,
  =.edn=, namespaces, and Clojure tooling.
- [[file:find-skills/SKILL.md][find-skills]] :: Discover and install agent skills from the
  open skills ecosystem via the Skills CLI. Triggers when users ask how to do
  something that might have an existing skill, ask to find a skill, or want to
  extend agent capabilities.
- [[file:gitlab-cli-skills/SKILL.md][gitlab-cli-skills]] :: Comprehensive GitLab
  CLI (=glab=) command reference and workflows. Covers GitLab auth, repos,
  issues, merge requests, CI/CD, releases, and related automation.
- [[file:skill-creator/SKILL.md][skill-creator]] :: Create, modify, and improve
  agent skills. Supports drafting new skills, running evals, benchmarking,
  iterating on behavior, and optimizing skill descriptions for triggering.
- [[./retro/SKILL.md][retro]] :: Session retrospective and self-improvement skill
