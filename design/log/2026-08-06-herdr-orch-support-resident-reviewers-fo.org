#+TITLE: herdr-orch: support resident reviewers for incremental review rounds
#+DATE: 2026-08-06 Thu
#+PARENT: [[archive:4957d0e5-5bda-4532-aa20-631fbde1983b][herdr-orch: support resident reviewers for incremental review rounds]]
#+SETUPFILE: ../../TASKS.setup.org
#+STATUS: Active
#+SPEC: [[proj:skills/herdr-orch/SKILL.md]]
#+SPEC: [[proj:skills/herdr-orch/scripts/docs/contract.md]]

* Intent

Let a parent keep a settled child (typically a reviewer or worker) and assign it follow-on rounds in its existing context, deciding continuation or closure explicitly after each published result. The child stays fully lifecycle-agnostic: it builds context, works, publishes, and either receives another assignment or gets closed.

Phase 2 pays down the cost of that explicitness. Making the parent decide after every capture added round trips and prose rules, so the ceremony the CLI can perform itself moves into the CLI: enforce contract details mechanically, and stop charging every spawn for instructions a guard could guarantee.

* Summary

*Level:* production · *Tests:* thorough · *Docs:* full

** Scope

*** In scope

- Remove automatic pane closure from every capture path (`run`, `collect`, `collect --wait`, `collect --any`); capture keeps validation, artifact links, and session backfill only.
- New `oh task close <full-task-uuid>` and `oh task close --settled` sweep: the explicit, guarded, auditable closure verb owning the relocated settle-wait machinery (`ORCH_SETTLE_CLOSE_MS`), closing only on a live child-name *and* pane-ID match and only from a child's newest entry.
- New `oh task continue <full-task-uuid> --task/--task-file/stdin [--wait]`: guarded reuse verb allocating a fresh task/result per round, new ledger entry with `:continues`, continuation prompt, `verify-dispatch!`.
- `publish`/`progress` gain a `--task` identity override (env `HERDR_ORCH_TASK` remains the default); the result path derives from the named ledger entry.
- Waiting policy becomes ledger-only: every entry records its own round's `:waiting-policy`, `publish!` reads it there, and env `HERDR_ORCH_WAITING_POLICY` is deleted from spawn env and from `publish!` altogether.
- contract.md and SKILL.md rewritten for the new lifecycle; sweep of packaged persona texts and cross-references asserting auto-close.
- `bb test` coverage for all of the above plus a continue-round live-smoke variant.
- Phase 2 (ceremony reduction) :: agent-facing output formats, a publish-side guard for a caller's own unclosed children, an orphan-cleanup verb, a `PROCESS`-candidate harvest, `collect --close`, and per-persona timeouts.
- Phase 2 (attendability) :: a second axis, not ceremony reduction -- focus follows the newest child on creation and returns to the parent when that child ends, reversing the shipped unfocused-spawn convention now that `:tab-split` gives each first-level child its own tab.

*** Out of scope

- Spawn-time residency flags, persona `retain:` frontmatter, capture-time `--retain` -- superseded by the explicit close/continue model.
- Envelope grammar or `collect --any` candidacy changes.
- Compatibility shims of any kind: this surface is closed-loop and repository-internal (`oh` writes every envelope and `oh` reads every envelope), so no legacy path, dual-read, or deprecation window is retained anywhere in this change.
- Any child-visible lifecycle state or child-chosen retention.
- Auto-resolving a stale publish identity to the newest round (hint only, never implicit re-targeting).
- The general summary/handoff revalidation rule, tracked by [[task:aaea2185-f6be-4260-8a9c-207b3a834866][Re-verify state claims before restating them in a summary]].

** Shipped

- ADDED :: `oh task close <full-task-uuid>` and `close --settled`; `oh task continue <full-task-uuid> [--wait]`; `--task` identity override on `publish`/`progress`; ledger fields `:continues`, `:closed-at`, per-round `:waiting-policy`; contract.md § Close and § Continue; `lifecycle-documentation-contract` static test; a continue-round live-smoke leg.
- MODIFIED :: capture (`run`, `collect`, `collect --wait`, `collect --any`) closes nothing; SKILL.md lifecycle and Class C (a closeout validator must be **spawned fresh**); README.org lifecycle prose and History; planner/worker persona texts.
- REMOVED :: capture-time auto-close and its settle wait; `HERDR_ORCH_WAITING_POLICY` (env and validation); the `unknown-ledger-entry` push half-case; the spawn prompt's "The waiting policy is …" line; dead-owner recovery; the unreachable `no-parent-pane` branch and the dead `FAKE_FAIL_CLOSE` fixture.
- CHANGED (follow-up) :: shipped `:defaults :placement` is now `:tab-split` -- a first-level child gets its own tab, grandchildren split inside it, which stays reviewable and recoverable under promiscuous spawning. `resolve-placement`'s `"split"` fallback deliberately stays: it fires only for a config declaring no `:placement`, which the mandatory shipped file always does. README.org documents all three values and the resolution order.
- CHANGED (closeout fixes) :: `close`'s shell_pid fallback (task ca6fecef) now also checks `process_info`'s `foreground_process_group_id`, retaining rather than closing a pane a non-agent foreground process (an operator's build, tail, or REPL) is actively occupying -- the fallback previously fell through to `close-mutation!` whenever no agent occupied the pane and the recorded `:shell-pid` matched, which was destructive on that scenario (P1). `assert-children-discharged!`'s publish-side "released" classification now shares this same shell_pid evidence instead of bare name-absence, so a delegating child can no longer publish while it still owns a live, protocol-reclaimable pane. `finish-capture!` and `live` now probe the child unconditionally rather than only `when-not (:child-session entry)`, so a bare `collect` of an already-published result refreshes a stale `:child-session` after a crash-and-resume (ecb85350's mainline-path gap). `record-session!` now short-circuits before `ledger/update!` for an unchanged observation, so the "no ledger write at all" claim (contract.md, cli.clj) is true rather than aspirational. SKILL.md, scripts/README.md, contract.md, and the CLI usage string are reworded to state the primary name-and-pane path plus the guarded fallback, rather than the fallback-blind absolute claim; `lifecycle-documentation-contract` bans that wording's return.

** Acceptance

*** Core functionality

- [X] No capture path (`run`, `collect`, `collect --wait`, `collect --any`) closes a pane; `maybe-close!`'s settle wait, status probe, and `herdr/close!` are gone from capture.
- [X] Capture still records `record-session!` backfill and foreign/owned reporting unchanged.
- [X] `oh task close <full-task-uuid>` closes a pane only on a live observation matching *both* the entry's `:child` name and its `:pane-id` in `idle`/`done`, only from the newest ledger entry for that child, after a bounded `ORCH_SETTLE_CLOSE_MS` wait; `:closed-at` is recorded on that newest entry. (Superseded 2026-08-07 at closeout, in the same way as the anti-criterion below: task `ca6fecef` added a second, explicitly guarded path -- a live pane whose recorded `:shell-pid` still matches closes even though the name match is unavailable, because a restarted process releases its agent name. The name-and-pane observation remains the *primary* path and the only one that fires when the name is present; the newest-round, ownership, and settle guards named here are unchanged, and the fallback additionally refuses a pane whose foreground process group differs from its shell.)
- [X] A child absent from a positively-usable `agent list` yields the non-destructive `{"status":"gone","reason":"child-absent"}` outcome and never closes the recorded pane. Absence proves only that the name was released, so the recorded `:pane-id` may host an unrelated replacement agent or user process -- the same reason `prune!` already refuses to close a pruned entry's pane. (Superseded 2026-08-07 at closeout by task ca6fecef's shell_pid fallback (§ Close): the retired reason string is `name-released`, not `child-absent`, and a live pane whose recorded `:shell-pid` still matches now closes rather than being retained as an unrelated replacement's pane -- "never closes the recorded pane" is therefore no longer an exhaustive claim about a name-absent child. It remains true that name absence *alone*, with no confirming shell_pid, still closes nothing.)
- [X] `close` refuses (loudly, no mutation): foreign or unresolvable caller identity (both `:parent-session`s must be non-nil), uncaptured entry, an uncaptured newer entry naming the same `:child`, or a stale round -- an entry that is not the newest for its child.
- [X] WITHDRAWN, not delivered :: this criterion required `close` to accept a "dead owner" -- an entry whose recorded `:parent-session` transcript no longer exists. Built as specified, then removed: the test never fires, because pi transcripts persist on disk indefinitely. Orphaned children are now an explicit operator-cleanup limitation documented in contract.md § Close, and `close` still refuses every foreign or unresolvable caller. Rationale and the rejected liveness signal are in Gotchas; the diagnosis is in Decisions.
- [X] `close` re-validates the freshest ledger state inside its final mutation, mirroring `prune!`'s race re-check.
- [X] An unsettled child yields the non-destructive `{"status":"retained","reason":"unsettled"}` outcome; close is retryable.
- [X] `oh task close --settled` takes no task argument (supplying one fails), groups owned captured entries by child across continuation lineage, considers only the newest entry per child, issues at most one close attempt per child, and returns a per-task outcome array.
- [X] `oh task continue <full-task-uuid>` allocates a fresh task UUID and result path and writes a new ledger entry inheriting `:child`/`:pane-id`/`:label`/`:persona-path` with `:continues <prior-task>`.
- [X] `continue` refuses (loudly, no mutation) unless: caller owns the entry, the prior entry is captured with a validated envelope (`COMPLETE`, `FAILED`, or `BLOCKED`), the live agent name *and* pane ID match `:child`/`:pane-id`, `agent_status` is `idle`/`done`, and no other uncaptured entry names that child.
- [X] `continue` is root-only: a caller with `HERDR_ORCH_PERSONA` set is refused before any ledger or pane mutation.
- [X] `continue` writes its new uncaptured ledger entry before prompting the pane, then runs `verify-dispatch!` after the prompt.
- [X] The continuation prompt names the new TASK, instructs publication with `--task`, identifies the assignment as a follow-on round, and requires revalidation of prior findings and mutable baselines before restating them.
- [X] `continue --wait` blocks like `run`; default is non-blocking like `start`. The chosen policy is written to the new entry's `:waiting-policy`, which is the sole input to both the prompt's guidance (progress instruction, parent push) and `publish!`'s notify/push behaviour for that round -- prompt text alone cannot change the mechanical path.
- [X] `publish --task <uuid>` overrides env `HERDR_ORCH_TASK`; the result path derives from the named ledger entry, and the entry's `:child` must equal env `HERDR_ORCH_CHILD`.
- [X] Waiting policy has exactly one home: the ledger entry's `:waiting-policy`. `HERDR_ORCH_WAITING_POLICY` is removed from the spawn env map (`cli.clj:395`) and from `publish!`'s resolution and validation (`cli.clj:518`, `:528`); no fallback path remains, so a continued round cannot carry a stale policy.
- [X] A `publish` whose task has no ledger entry is silent -- no toast, no parent push -- because a publication with no entry has no parent to advise; this replaces the current `{"push":"skipped","reason":"unknown-ledger-entry"}` half-case (`cli.clj:537-538`) with one uniform rule and is a deliberate narrowing of the hand-driven `publish` in SKILL.md § Manual fallback.
- [X] `progress --task <uuid>` applies the same override and validation.
- [X] A publish that fails because the env-derived task is already published, when exactly one uncaptured entry names the same child, emits an error naming the fix: retry with `--task <uuid>`.
- [X] contract.md documents: the close and continue verbs with guards and refusal cases, the `--settled` sweep's one-close-per-child grouping, the `gone` and dead-owner outcomes, `publish`/`progress` `--task`, ledger fields `:continues`/`:closed-at`/per-round `:waiting-policy`, the removal of `HERDR_ORCH_WAITING_POLICY` from the child env table, the silent ledger-less publish, and capture's non-closing semantics, and that a continued entry's `:child` name does not derive from its own task UUID.
- [X] SKILL.md lifecycle text states: panes persist until the parent closes or continues them; close-or-continue after every capture; and Class C is *rewritten* to require the final delegated validator to be newly spawned for that closeout and never a child continued from an earlier round, retaining the existing prohibition on an implementing worker reaffirming its own result.
- [X] `bb test` passes with new coverage for capture-no-close, close guards and sweep, continue guards and prompt, and publish/progress overrides -- including a replacement agent occupying a recorded `:pane-id`, two captured continuation rounds of one child, a stale-round close attempt, one-close-per-child sweep behaviour, a dead-owner close, and both policy transitions (`run` -> `continue`, `start` -> `continue --wait`) asserted on notify/push behaviour rather than prompt text alone.
- [X] The live smoke gains a continue-round variant proving same-pane context continuation end-to-end.

*** Edge cases

- [X] `continue` on an `invalid`, uncaptured, or pruned prior entry: refused.
- [X] `continue` on a captured `BLOCKED` envelope whose agent is settled `idle`/`done`: accepted (unified blocked-resumption).
- [X] `close` on an entry whose agent has vanished from a positively-usable listing: reported `gone`, pane retained, nothing mutated; an unusable or failed listing is equally non-destructive. A vanished child's pane is an accepted manual-cleanup case, deliberately not an automatic close.
- [X] `close` on an entry whose recorded `:pane-id` is now occupied by a different live agent: refused, pane untouched -- the child-name-and-pane-ID match fails.
- [X] `close --settled` with two captured rounds of the same child: exactly one close attempt, against the newest entry only.
- [X] Foreign-session capture behaviour unchanged.
- [X] `prune` and `collect --any` candidacy treat continuation entries as ordinary entries.
- [X] The parent-push advisory names close-or-continue alongside the collect command.

*** Ceremony reduction (phase 2)

- [X] Delegation verbs (`collect`, `status`, `list`) accept `--format text` and stop emitting the parsed envelope *and* a duplicate raw `text` blob in the same payload; the raw envelope is opt-in. `list --format text` renders one line per ledger *entry* -- that is, one per round: task, child, pane, status, captured/uncaptured, closed/open, task id first because it is the only field a caller can act on. (Corrected 2026-08-07 at closeout from "one line per child", which the delivered code never matched and which was wrong as a criterion: a continued child holds one entry per round, and collapsing its rounds onto one line would hide exactly the history `continue` exists to create. Code and contract.md were correct as they stood; only the criterion was inaccurate.)
- [X] `publish` refuses, or loudly warns, when the caller still owns an uncaptured or unclosed child in the ledger -- the rule SKILL.md currently states only in prose.
- [X] An orphan-cleanup verb replaces the documented two-step `agent list` then `pane close` recipe, applying the same live child+pane match with operator-supplied authority.
- [X] A verb harvests this session's `PROCESS` candidates from the ledger, deduplicated, so a retro does not depend on re-reading every capture.
- [X] `collect --close` performs capture and closure in one call without weakening the explicit close-or-continue decision.
- [X] Persona frontmatter carries `timeout:`, replacing the standing "give review and implementation work an explicit `--timeout`" instruction.
- [X] Ledger growth is bounded by an ownership-aware retention path, without destroying the post-closure transcript references that make an old entry worth keeping.
- [X] Spawning focuses the new child, focus returns to the parent when that child ends, both are configurable, and every document asserting unfocused spawning is corrected.
- [X] Every rule these changes make mechanical is *deleted* from SKILL.md, not merely restated alongside the guard. Net prose must shrink. Measured: 3348 -> 3338 words, with four instructions deleted (close-before-publish, explicit `--timeout`, the two-step orphan recipe, harvest-by-session) against one added clause naming `collect --close`; `lifecycle-documentation-contract` pins both the deletions and the word-count ceiling.

*** Anti-criteria

- [X] Must not: close a pane on evidence weaker than a live observation matching both the child name and the pane ID -- name absence, a stale ledger `:pane-id`, or result prose are never sufficient. (Superseded 2026-08-07 at closeout: task ca6fecef added the guarded shell_pid fallback for a released name, § Close, which closes on a live pane id plus a matching recorded `:shell-pid` alone, deliberately weaker than this anti-criterion's bar. Name absence alone, a stale `:pane-id` alone, and result prose remain never sufficient -- only that narrower reading of this line survives.)
- [X] Must not: infer continuation or closure from `NEXT:` or any result prose.
- [X] Must not: relax per-task one-shot result publication (hard-link once; pre-existing result stays an error).
- [X] Must not: make the child aware of, or able to choose, its own lifecycle (nothing lifecycle-related in spawn env or spawn prompt).
- [X] Must not: auto-resolve a stale publish identity to another entry.
- [X] Must not: change the envelope grammar -- not for compatibility, but because this design needs no grammar change and altering it would forfeit free reuse of `collect`, validation, and fan-in.
- [X] Must not: change the `collect --any` candidate predicate; continuation entries must remain ordinary candidates or fan-in silently miscounts.
- [X] Must not: leave two sources of truth for any lifecycle value -- policy, result path, and pane identity each resolve from the ledger entry alone.

** Decisions

- Design principles: flexibility, robustness, simplicity, minimal runtime checks on hot paths.
- Mechanical enforcement over instruction :: where a contract detail can be enforced by the CLI -- derived from the ledger, validated at the boundary, or refused loudly -- it must not instead be delegated to prompt text the child is asked to obey. Every rule moved into code removes tokens from every future spawn and removes a failure mode where the child simply does not comply. Prompts carry assignment content and one exact command to run; they do not carry protocol the CLI could have guaranteed.
- Chose explicit per-publication lifecycle (no auto-close, `close`/`continue` verbs) :: session evidence showed (a) the retain decision is best made after reading the published result, where no spawn-time flag can serve, and (b) auto-close failure is silent *by construction* -- wait, probe, and close errors are swallowed and never alter any result field (`cli.clj:330-340`, asserted by `cli_test.clj:1719` and `cli_test.clj:1828`) -- so a parent can never rely on it and must enforce closure anyway. Failure *frequency* is unmeasured and deliberately not claimed: no rate exists in source, tests, docs, or task history, and the argument rests on unreliability-by-construction alone.
  Rejected:
  - Spawn-time `--resident` and/or persona `retain:` frontmatter with capture-time `--retain` promotion: prediction-based; solves the wrong moment; three-layer resolution surface for no gain once closure is explicit.
  - Ephemeral-by-default with retention flags: preserves the unreliable auto-close and the settle-wait on every capture.
  - Multi-publish to one result path: breaks one-shot atomicity; per-round fresh task/result reuses collect, validation, and fan-in unchanged.
- One-shot publication is retained per task, relaxed per child lifetime: a child may publish once per assigned round; continuation allocates the next round rather than republishing.
- `--task` overrides identity; the ledger owns policy :: `publish!` currently reads `HERDR_ORCH_WAITING_POLICY` from env (`cli.clj:518`, validated `:528`) while spawn injects it exactly once (`cli.clj:395`), so a continued round would publish under its spawn-time policy -- a `start`-spawned child continued with `--wait` taking the non-blocking push path, a `run`-spawned child continued non-blocking publishing with no advisory at all. Rather than add an env fallback, the env var is deleted and the entry's `:waiting-policy` becomes the single home for the value. A stale policy then cannot be represented, which is stronger than validating against one. A mistyped `--task` can still only reach the same child's own rounds, because `:child` must match env `HERDR_ORCH_CHILD`, which is stable across rounds.
- No compatibility constraints, by design :: `oh` writes every envelope and `oh` reads every one, so `herdr-orch/v1` has no external consumer and the schema version is free to move whenever clarity demands it. The earlier "must not change the machine schema version" anti-criterion protected nothing and was removed; the envelope grammar stays frozen only because this design needs no grammar change, and the `collect --any` predicate stays frozen because fan-in correctness depends on it. Simplicity and robustness are the only constraints that survive: no legacy path, dual-read, or deprecation window anywhere.
- Herdr floor is an observation, not a constraint :: every call this design uses (`pane close`, `agent get`/`list`/`prompt`/`wait`) shipped in 0.7.5, so the existing version gate in `herdr.clj` needs no change. Recorded here rather than as an anti-criterion, since no implementer has to engineer for it.
- Stale-identity failures hint, never auto-resolve: implicit re-targeting would reintroduce hidden lifecycle magic.
- `continue` doubles as the blocked-resumption verb: resuming a captured `BLOCKED` envelope requires a fresh task anyway; envelope-status `BLOCKED` is distinct from agent-status `blocked`, and the `idle`/`done` delivery guard prevents typing into an approval UI.
- Constitution note: this change rewrites the skill's own documented lifecycle contract (SKILL.md + contract.md); that is the point of the change, not silent divergence.
- Class C must be rewritten, not retained verbatim :: `SKILL.md:98` says only "the root or an ephemeral reviewer" and never says *fresh*; it inherits that meaning from the adjacent "ephemeral by default" lifecycle default at `SKILL.md:50`, which this change removes. Preserving the sentence byte-identically would therefore silently drop the freshness guarantee, letting a long-resident reviewer reaffirm its own earlier review at feature closeout -- exactly what parent task `4957d0e5` forbids for high-consequence closure.
- Focus moves only on a root-initiated action :: two decisions taken together, and the invariant is the point of both. **Depth:** only a root spawn focuses its child; a below-root spawn never does, gated on the existing `:below-root?` predicate (`HERDR_ORCH_PERSONA`) rather than a second notion of depth that could drift from it. A root spawn is the operator's proxy -- they asked for the child; a below-root spawn happens at a moment no human chose, and its blocking, one-at-a-time grandchild is visible from its parent's pane anyway. **Return hook:** `close` returns focus and `collect --close` inherits it; `publish` never moves focus. `close` runs in the parent's own process, so the pane is its own `$HERDR_PANE_ID`; it has already proven the child finished; and under fan-out closes are serialised by the parent's turn where publishes arrive independently and would each yank the screen. Sweeps focus once, after the sweep, never per child.
  Rejected:
  - Publish-time return: a publishing child routinely finds its parent `working`, `blocked`, or session-mismatched (`push-decision`), which is precisely the mid-read theft the anti-criterion forbids; and publication is already committed before `notify-parent!` runs, whose contract is that every outcome is *only reported*. A UI mutation does not belong on the one path that must never fail. Timeliness is already served by the advisory push.
  - Literal "most recently spawned": lets a worker's own consult steal the screen mid-read.
  Corrected at closeout: the return hook shipped ungated, and because the publish guard *forces* a delegating child to close its grandchildren before publishing, every nested delegation ended in a child-initiated focus steal. Both closeout validators found it independently. The criterion, not the decision, was underspecified.
- Closure gained a second witness rather than an operator override :: a restarted process releases its Herdr agent name, so the name-and-pane match can never see it again and every close path reported `gone` forever, stranding a live pane outside the protocol. The route the plan anticipated -- reading `HERDR_ORCH_TASK` back from the pane -- was measured impossible: Herdr exposes no pane environment, and Darwin yields none from a pid. A pane's `shell_pid` works instead, because the shell is the *parent* of whatever agent occupies the pane and therefore survives the agent restarting in place. It is strictly a fallback: it never fires when the name is present, never relaxes the ownership or newest-round guards, and refuses a pane whose foreground process group differs from its shell, since that means an operator is using it. Rejected: a new operator-authorised override verb -- the witness covers the case it existed for, and the residual (no recorded value, a mismatched shell, genuine pane-id reuse) is reported as `name-released` with a remedy rather than given a verb that closes on weaker evidence.
- Revised after design review (`.tmp/herdr-orch/40fcdfe3-100a-4975-a956-b07e4c087af4-design-review.md`, verdict NEEDS CHANGES) :: close-safety, waiting-policy relocation, the Class C rewrite, dead-owner recovery, plan serialisation, and the `prune!` reference were all corrected here; the record returned to `Review` for sign-off.

** Gotchas

- A pane outlives its agent, so name-absence *alone* is never permission to close -- but it is not proof of death either, and treating it as such stranded live children. A restarted process releases its agent name, so every close path reported `gone` forever and the pane could only be reclaimed outside the protocol. The resolution is a second witness: a pane's shell is the *parent* of whatever agent occupies it, so `shell_pid` (recorded at spawn, read back via `pane process-info`) survives the agent dying and restarting in place. Measured, because the obvious route does not exist: Herdr exposes no pane environment, and on Darwin `ps eww` returns only the header and command line even for the caller's own process. Accepted trade: a leaked pane still beats a wrongly killed one, so the fallback fires only on a live, matching shell whose foreground process group equals that shell -- a busy foreground means an operator is using the pane, and it is retained.
- **Dead-owner recovery was specified, built, and removed.** The original criterion accepted a close when the recorded `:parent-session` transcript "no longer exists" -- which never becomes false, because pi transcripts persist on disk indefinitely. It passed every unit test, because fixtures write transcript paths the test itself controls, so the fixture proved the code matched the spec rather than the spec matching reality. Caught only when a real orphan appeared in a real sweep. Consequence at the time: an orphaned child whose owning session ended could not be closed through the protocol at all. Phase 2's `orphans` verb later gave the operator that path under close's own guards, so the gap is now bounded rather than absolute. `agent list` does expose `agent_session`, but absence from one listing proves only current detachment, so a liveness-based rebuild needs a sounder signal than that.
- **A doc-contract test polices only the files it lists.** `lifecycle-documentation-contract` passed green while README.org still asserted capture-time closure, because README.org was absent from its `lifecycle-docs` vector. Adding the file is half the fix; the banned-phrase list must also name the wording each stale document actually used. Ban the false subject ("Every capture path settle-waits"), never a shared predicate ("before its single close attempt") that is true of `close`.
- Pane accumulation is now the parent's problem by design. `close --settled` is the cheap sweep; it exits 0 while reporting per-child refusals inside its array, so a caller checking only the exit code will read a sweep-with-refusals as fully successful.
- Root cannot close a grandchild it does not own, so the smoke's nested leg *instructs* its planner child to close its own scout. That is an LLM-compliance dependency rather than a mechanical guard -- loud on failure, but a real flakiness surface.
- Anchoring in long residencies is mitigated by the continuation prompt's revalidation clause and by the rewritten Class C rule; independence is a property of the spawn, not of the instruction.
- **A guard wrapped in a presence check is vacuous exactly where it matters -- three instances in one change.** Dead-owner tested a file that always exists; `lifecycle-documentation-contract` swept only the files its own list named; and `publish`'s child-identity check was wrapped in `(and entry ...)`, so an explicit `--task` naming an unknown entry skipped it and fell through to the env result path, burning the child's real one-shot round. All three read correct, passed review, and passed a green suite. When a guard's condition includes "if we found the thing", ask what happens when the thing is absent -- that is usually the case the guard exists for.
- **Parallel review with two models found disjoint defect sets and opposite verdicts.** One returned APPROVED with no P0-P2; the other returned NEEDS CHANGES with four P2s, every one confirmed at source. They agreed on a single line of one file. Single-reviewer sign-off on this surface is not sufficient evidence. Repeated at final closeout with a different result -- both validators returned NEEDS CHANGES and converged on four of five findings -- so overlap varies with the surface, and neither agreement nor disagreement between two reviewers is itself the signal.
- **A regression pin sized to one change's savings blocks the next change's documentation.** The SKILL.md prose guard was calibrated to phase 2's specific ceremony-to-CLI savings, so the first feature round that legitimately needed new documentation had to raise it. Worse, it was named for a *direction* ("net prose shrank") while asserting only a ceiling, so a passing suite reported shrinkage while the file had grown ~4%. Size a prose-length pin as a delta against its own moving baseline, and name a guard for what it enforces, not for the outcome that motivated it.
- **A live-smoke fixture must not need production text to work.** The retro leg planted a fault and failed the run unless the child emitted a candidate about it -- a threshold judgement, not a mechanical fact. It passed or failed depending on whether the child noticed the plant was artificial, so it rewarded non-compliance, and the skill carried prose describing the fixture purely to keep it working. Assert the mechanical half (the gate resolved, the opt-out leg stayed silent) and prove the rest deterministically in a unit test.

* Plan

** DONE Remove pane closure from every capture path
CLOSED: [2026-08-06 Thu 22:21]
:PROPERTIES:
:CUSTOM_ID: e55d18e4-7b26-47c2-80c6-0efd66bf7760
:CREATED: [2026-08-06 Thu 21:08]
:BLOCKED-BY: task:f903bd82-b26a-4187-a3aa-dd8696678e69
:BLOCKED-BY+: task:95705ac5-0e94-4422-9195-fe44c93f37da
:STARTED: [2026-08-06 Thu 22:11]
:END:
:LOGBOOK:
- State "STARTED" from "TODO" [2026-08-06 Thu 22:11]
- State "DONE" from "STARTED" [2026-08-06 Thu 22:21]
:END:

** DONE Add `oh task close` with `--settled` sweep
CLOSED: [2026-08-06 Thu 21:59]
:PROPERTIES:
:CUSTOM_ID: f903bd82-b26a-4187-a3aa-dd8696678e69
:CREATED: [2026-08-06 Thu 21:08]
:BLOCKED-BY: task:c165b881-6faf-493a-bb23-b9d2fa09221f
:STARTED: [2026-08-06 Thu 21:51]
:END:
:LOGBOOK:
- State "STARTED" from "TODO" [2026-08-06 Thu 21:51]
- State "DONE" from "STARTED" [2026-08-06 Thu 21:59]
:END:

** DONE Add `--task` identity override to `publish` and `progress`
CLOSED: [2026-08-06 Thu 21:51]
:PROPERTIES:
:CUSTOM_ID: c165b881-6faf-493a-bb23-b9d2fa09221f
:CREATED: [2026-08-06 Thu 21:08]
:STARTED: [2026-08-06 Thu 21:42]
:END:
:LOGBOOK:
- State "STARTED" from "TODO" [2026-08-06 Thu 21:42]
- State "DONE" from "STARTED" [2026-08-06 Thu 21:51]
:END:

** DONE Add `oh task continue` for follow-on rounds
CLOSED: [2026-08-06 Thu 22:11]
:PROPERTIES:
:CUSTOM_ID: 95705ac5-0e94-4422-9195-fe44c93f37da
:CREATED: [2026-08-06 Thu 21:08]
:BLOCKED-BY: task:c165b881-6faf-493a-bb23-b9d2fa09221f
:BLOCKED-BY+: task:f903bd82-b26a-4187-a3aa-dd8696678e69
:STARTED: [2026-08-06 Thu 21:59]
:END:
:LOGBOOK:
- State "STARTED" from "TODO" [2026-08-06 Thu 21:59]
- State "DONE" from "STARTED" [2026-08-06 Thu 22:11]
:END:

** DONE Rewrite lifecycle docs and sweep auto-close references
CLOSED: [2026-08-06 Thu 23:02]
:PROPERTIES:
:CUSTOM_ID: ed671083-2542-45e8-9ca1-9377ac337182
:CREATED: [2026-08-06 Thu 21:08]
:BLOCKED-BY: task:e55d18e4-7b26-47c2-80c6-0efd66bf7760
:BLOCKED-BY+: task:f903bd82-b26a-4187-a3aa-dd8696678e69
:BLOCKED-BY+: task:c165b881-6faf-493a-bb23-b9d2fa09221f
:BLOCKED-BY+: task:95705ac5-0e94-4422-9195-fe44c93f37da
:BLOCKED-BY+: task:f8ea9243-798c-4590-8113-5a29ef0bffdd
:STARTED: [2026-08-06 Thu 22:39]
:END:
:LOGBOOK:
- State "STARTED" from "TODO" [2026-08-06 Thu 22:39]
- State "DONE" from "STARTED" [2026-08-06 Thu 23:02]
:END:

** DONE Live-smoke continue-round variant
CLOSED: [2026-08-06 Thu 22:38]
:PROPERTIES:
:CUSTOM_ID: f8ea9243-798c-4590-8113-5a29ef0bffdd
:CREATED: [2026-08-06 Thu 21:08]
:BLOCKED-BY: task:95705ac5-0e94-4422-9195-fe44c93f37da
:BLOCKED-BY+: task:f903bd82-b26a-4187-a3aa-dd8696678e69
:BLOCKED-BY+: task:e55d18e4-7b26-47c2-80c6-0efd66bf7760
:END:
:LOGBOOK:
- State "DONE" from "TODO" [2026-08-06 Thu 22:38]
:END:

** DONE Agent-facing output: `--format text` and no duplicated envelope
CLOSED: [2026-08-07 Fri 01:15]
:PROPERTIES:
:CUSTOM_ID: f6cc6df4-660e-4da9-8971-35a54771e129
:CREATED: [2026-08-07 Fri 00:40]
:STARTED: [2026-08-07 Fri 01:10]
:END:
:LOGBOOK:
- State "STARTED" from "TODO" [2026-08-07 Fri 01:10]
- State "DONE" from "STARTED" [2026-08-07 Fri 01:15]
:END:

** DONE Guard a publishing child's own unclosed children
CLOSED: [2026-08-07 Fri 01:34]
:PROPERTIES:
:CUSTOM_ID: 4f29615a-ddfe-47c6-ab6b-28c0623c94bf
:CREATED: [2026-08-07 Fri 00:40]
:STARTED: [2026-08-07 Fri 01:22]
:END:
:LOGBOOK:
- State "STARTED" from "TODO" [2026-08-07 Fri 01:22]
- State "DONE" from "STARTED" [2026-08-07 Fri 01:34]
:END:

** DONE Orphan-cleanup verb
CLOSED: [2026-08-07 Fri 01:41]
:PROPERTIES:
:CUSTOM_ID: d39fa580-ff6e-4f97-89cc-2398fa7c26e1
:CREATED: [2026-08-07 Fri 00:40]
:STARTED: [2026-08-07 Fri 01:34]
:END:
:LOGBOOK:
- State "STARTED" from "TODO" [2026-08-07 Fri 01:34]
- State "DONE" from "STARTED" [2026-08-07 Fri 01:41]
:END:

** DONE Harvest `PROCESS` candidates from the ledger
CLOSED: [2026-08-07 Fri 02:14]
:PROPERTIES:
:CUSTOM_ID: 7afb2c33-d916-4a65-a67f-6478e8ae05d0
:CREATED: [2026-08-07 Fri 00:40]
:STARTED: [2026-08-07 Fri 02:05]
:END:
:LOGBOOK:
- State "STARTED" from "TODO" [2026-08-07 Fri 02:05]
- State "DONE" from "STARTED" [2026-08-07 Fri 02:14]
:END:

** DONE `collect --close`
CLOSED: [2026-08-07 Fri 01:22]
:PROPERTIES:
:CUSTOM_ID: 339bd67e-0359-4fc8-9aff-dea3c4afa7a9
:CREATED: [2026-08-07 Fri 00:40]
:STARTED: [2026-08-07 Fri 01:15]
:END:
:LOGBOOK:
- State "STARTED" from "TODO" [2026-08-07 Fri 01:15]
- State "DONE" from "STARTED" [2026-08-07 Fri 01:22]
:END:

** DONE Persona `timeout:` frontmatter
CLOSED: [2026-08-07 Fri 02:05]
:PROPERTIES:
:CUSTOM_ID: 343f80c8-3734-49de-9e4e-8573c4f8e819
:CREATED: [2026-08-07 Fri 00:40]
:STARTED: [2026-08-07 Fri 01:51]
:END:
:LOGBOOK:
- State "STARTED" from "TODO" [2026-08-07 Fri 01:51]
- State "DONE" from "STARTED" [2026-08-07 Fri 02:05]
:END:

** DONE Bound ledger growth without losing the audit trail
CLOSED: [2026-08-07 Fri 01:51]
:PROPERTIES:
:CUSTOM_ID: ac65f6ae-7796-4e29-ab4b-5ecfee14d287
:CREATED: [2026-08-07 Fri 01:00]
:STARTED: [2026-08-07 Fri 01:41]
:END:
:LOGBOOK:
- State "STARTED" from "TODO" [2026-08-07 Fri 01:41]
- State "DONE" from "STARTED" [2026-08-07 Fri 01:51]
:END:

** DONE Focus follows the newest child, and returns when it ends
CLOSED: [2026-08-07 Fri 09:18]
:PROPERTIES:
:CUSTOM_ID: 8869bd4f-91b5-49c2-bf7f-13118591b329
:CREATED: [2026-08-07 Fri 01:08]
:STARTED: [2026-08-07 Fri 08:38]
:END:
:LOGBOOK:
- State "STARTED" from "TODO" [2026-08-07 Fri 08:38]
- State "DONE" from "STARTED" [2026-08-07 Fri 09:18]
:END:

** DONE A resumed child is permanently unclosable by the protocol
CLOSED: [2026-08-07 Fri 14:13]
:PROPERTIES:
:CUSTOM_ID: ca6fecef-7264-4707-af13-d6b51639a8b6
:CREATED: [2026-08-07 Fri 12:56]
:STARTED: [2026-08-07 Fri 13:24]
:END:
:LOGBOOK:
- State "STARTED" from "TODO" [2026-08-07 Fri 13:24]
- State "DONE" from "STARTED" [2026-08-07 Fri 14:13]
:END:

** DONE Capture never corrects a `:child-session` that a resume made stale
CLOSED: [2026-08-07 Fri 13:56]
:PROPERTIES:
:CUSTOM_ID: ecb85350-5715-4e0b-a454-4ed2fa95703a
:CREATED: [2026-08-07 Fri 12:56]
:STARTED: [2026-08-07 Fri 13:24]
:END:
:LOGBOOK:
- State "STARTED" from "TODO" [2026-08-07 Fri 13:24]
- State "DONE" from "STARTED" [2026-08-07 Fri 13:56]
:END:

** DONE The live smoke's retro gate depends on the child's own judgement
CLOSED: [2026-08-07 Fri 13:44]
:PROPERTIES:
:CUSTOM_ID: 377ad650-603e-46a0-a5ce-ce5473d8367e
:CREATED: [2026-08-07 Fri 12:56]
:STARTED: [2026-08-07 Fri 13:24]
:END:
:LOGBOOK:
- State "STARTED" from "TODO" [2026-08-07 Fri 13:24]
- State "DONE" from "STARTED" [2026-08-07 Fri 13:44]
:END:

* Implementation

`bb test`: 457 tests / 2680 assertions at baseline, 533 / 3313 at closeout; 0 failures throughout. Live smoke green on the final tree, covering the retro, nested, and continuation legs with no leaked panes.

Delivered by delegation across seven worker rounds, gated at each milestone by validators spawned fresh. Every review round found real defects a green suite did not: the design review corrected close-safety and the Class C rule before code existed; post-merge review found four; the first closeout pass found seven; the final pass found five, including a destructive regression in the shell_pid fallback. No milestone was clean on first inspection, and three separate defects were guards wrapped in a presence check that made them vacuous exactly where they mattered.

The change dogfooded itself throughout, which is where its last three tasks came from. A crash killed root and its worker mid-round; the worker was resumed, finished, and published a valid result that no close path would then accept, because restarting had released its agent name. That produced `ca6fecef`, `ecb85350`, and `377ad650` -- none reachable from review or from a passing suite.
