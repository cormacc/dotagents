#+TITLE: Modernize pi extensions for pi 0.80 APIs
#+DATE: 2026-07-20 Mon
#+PARENT: [[archive:a763d8fc-0dfb-4207-b957-1573b05a04b9][Modernize pi extensions for pi 0.80 APIs]]
#+SETUPFILE: ../../TASKS.setup.org
#+STATUS: Complete
#+SPEC: [[proj:pi/skills/ext-dev/SKILL.md]]
#+SPEC: [[proj:skills/clojure/SKILL.md]]
#+SPEC: [[proj:skills/clojure/references/tool-guide.md]]

* Intent
Adopt the extension-facing capabilities and lifecycle guarantees added from pi 0.69.0 through 0.80.10 where they remove local workarounds, prevent incorrect behavior, or reduce model context cost. Keep each extension's existing purpose and user-facing workflow while moving maintained code onto current public APIs and explicit TUI/RPC boundaries. Treat =pi-clojure= as locally owned code: preserve its import history and license attribution, but optimize and document it according to this repository's current contracts rather than an upstream-sync constraint.

* Summary
*Level:* production · *Tests:* thorough · *Docs:* README

** Scope
*** In scope
- Make the question tool sequential and explicitly TUI-only while preserving cancellation and free-text behavior.
- Guard the tasks overlay with =ctx.mode= without disabling RPC-capable task creation and doctor commands.
- Replace stale event-bus registrations with subscriptions that are released during =session_shutdown=.
- Move display-only system-prompt output from model-visible messages to custom entries, with an explicit non-TUI behavior.
- Return standard pi diffs for LSP rename operations and invalidate cached file previews after mutations and session replacement.
- Extend the reviewed changelog window back to pi 0.69.0 (2026-04-22) so the TypeBox 1.x migration and other extension-facing changes immediately preceding May are explicitly covered.
- Resolve global extension files through =getAgentDir()= and migrate maintained imports from =@mariozechner/*= to =@earendil-works/*= and from the legacy-aliased =@sinclair/typebox= root to =typebox=.
- Harden and simplify the now locally owned =pi-clojure= extension: current public types, bounded/cancellable nREPL operations, deterministic transport cleanup, stronger tests, and ownership-accurate documentation.
- Replace the Herdr agent-state retry timing heuristic through the managed integration's source of truth, or capture an upstream follow-up when that source cannot be changed here.
- Update =pi/skills/ext-dev/SKILL.md=, =skills/clojure/SKILL.md=, the Clojure tool guide, and extension READMEs with the durable API, mode, and tool behavior rules exercised by the changes.
*** Out of scope
- Dynamic browser-tool loading without measured prompt-cache pressure.
- Jira issue-key autocomplete without a product request and cache/search design.
- Provider registration, =ModelRuntime=, compaction hooks, project-trust policy, or SDK embedding changes not used by these extensions.
- Replacing the intentional model-visible source injection in =ext-dev= with a display-only entry.
- Broad rewrites of extension UI or tool schemas unrelated to the reviewed pi changes.

** Acceptance
*** Core functionality
- [X] Multiple =question= calls in one assistant turn execute one at a time.
- [X] =question= reports unsupported mode outside TUI instead of reporting a false user cancellation.
- [X] The =/tasks= overlay opens only in TUI mode.
- [X] RPC-capable =/tasks new= and =/tasks doctor= behavior remains available.
- [X] Reloading or replacing a session does not accumulate =emacs:open= or =tasks:status-changed= listeners.
- [X] TUI =/systemprompt= remains human-visible without adding the prompt to later model context.
- [X] RPC =/systemprompt= returns the prompt through supported RPC UI output without creating a context message.
- [X] JSON and print mode =/systemprompt= exit without appending a session entry or context message, and their unsupported behavior is documented.
- [X] LSP rename results contain bounded standard before/after diffs for every modified file.
- [X] LSP location previews reflect file contents after extension-driven mutations and session replacement.
- [X] Jira and tasks global settings and the pi-side org-tasks fallback resolve beneath =getAgentDir()=.
- [X] Maintained extension imports use the current =@earendil-works/*= package scope and =typebox= 1.x root.
- [X] =pi-clojure= uses current pi/typebox declarations without locally redeclaring the coding-agent API.
- [X] =clojure_eval= can be cancelled and its timeout covers connection establishment and response processing.
- [X] =pi-clojure= tool results are bounded before entering model context or session details.
- [X] The external Herdr canonical source and required =agent_settled=/listener-cleanup changes are owned by linked follow-up [[task:a6be7668-26a2-4b12-b2b9-c40d07af55dc][a6be7668]]; generated local output remains untouched.
- [X] Modified extension documentation and all three declared specs describe the resulting mode, lifecycle, context-visibility, and nREPL contracts.
- [X] Test modules are nested outside the extension auto-discovery root, and validation rejects future root-level test modules.
*** Edge cases
- [X] Unsubscribing listeners is idempotent during reload, session switch, and process shutdown.
- [X] A cancelled or timed-out nREPL operation settles once, closes its socket, and cannot later emit a second result.
- [X] Chunked and nested bencode responses remain decodable after transport cleanup changes.
- [X] Multi-file LSP renames keep file-mutation queue ordering and bounded tool output.
*** Anti-criteria
- [X] Must not: edit =pi/extensions/herdr-agent-state.ts= as the canonical source when Herdr will overwrite it.
- [X] Must not: remove source content from the model context produced by =/ext=.
- [X] Must not: disable task creation or doctor reporting merely because the caller is RPC rather than TUI.
- [X] Must not: add a new runtime dependency for these migrations.
- [X] Must not: activate speculative dynamic-tool loading or Jira autocomplete as part of this task.

** Decisions
- Chose targeted API adoption with regression tests per affected extension :: it captures the concrete pi 0.69.0–0.80.10 wins without coupling unrelated extensions into a framework rewrite.
  Rejected:
  - Rebase each extension wholesale on current upstream examples: local behavior and integrations would be overwritten or obscured.
  - Redesign the complete tool surface around dynamic loading: added indirection is not justified by the current tool counts or measured cache data.
- Preserve mode-appropriate behavior rather than treating =ctx.hasUI= as equivalent to TUI :: RPC supports dialogs and notifications but not custom component factories.
- Use custom entries only for human-facing data that should not enter model context :: =systemprompt= qualifies; =ext-dev-source= intentionally does not.
- Use =getAgentDir()= for global files and reserve =CONFIG_DIR_NAME= for project-local config paths :: this preserves configured agent-directory overrides.
- Treat the Herdr file header as authoritative :: change the integration generator/source when discoverable; otherwise create a concrete upstream follow-up rather than committing a change that reinstalling Herdr will erase.
- Include the pi 0.69.0 TypeBox migration despite its 2026-04-22 date :: it is the nearest preceding extension break, the local tree still depends on the compatibility alias, and the user explicitly expanded the review window beyond May.
- The pi 0.69.0–0.71.0 review found no current use for terminating tool results, stacked autocomplete, =message_end= replacement, custom-editor composition, thinking-level events, or working-row visibility :: retain them as reviewed/non-applicable rather than manufacturing features.
- Migrate old package scopes, including =@sinclair/typebox= → =typebox=, but keep =StringEnum= on the current pi-ai root :: none of the extensions use the legacy global model API that moved to =/compat=; pi bundles typebox 1.1.38 and no longer shims the =@sinclair/typebox/compiler= subpath.
- Maintained scope is every extension under =pi/extensions/= except the Herdr-managed =herdr-agent-state.ts= :: =pi-clojure= began as a copied upstream project but has been materially modified and is now owned here, so behavioral simplification, transport hardening, tests, and documentation are in scope while original license and authorship attribution remain intact.

** Shipped
- MODIFIED =pi/extensions/emacsclient= and =pi/extensions/jira= to release shared event-bus subscriptions on =session_shutdown=; a regression test covers reload, replacement, and idempotent cleanup.
- MODIFIED =pi/extensions/systemprompt.ts= so TUI display uses a persisted custom entry, RPC uses UI notification output, and JSON/print remain side-effect free; the prompt no longer enters model context.
- MODIFIED =pi/extensions/lsp= rename handling to return bounded standard before/after diffs and invalidate location-preview cache entries after mutations and session shutdown.
- MODIFIED maintained extension dependencies/imports to current =@earendil-works/*= and TypeBox 1.x packages; Jira/tasks settings and the pi-side org-tasks fallback now honor the configured agent directory.
- MODIFIED the owned =pi-clojure= extension with cancellable, single-settlement nREPL transport, bounded discovery/results, validated schemas, comprehensive transport/tool tests, and ownership-accurate documentation.
- MODIFIED =skills/clojure/SKILL.md= and =skills/clojure/references/tool-guide.md= for the native tools' timeout, cancellation, discovery-budget, and output-bound contracts.
- REMOVED the vendoring-oriented =pi/extensions/pi-clojure/FORK-CHANGES.md= name in favor of =HISTORY.md= while preserving import and license attribution.
- MODIFIED =pi/skills/ext-dev/SKILL.md= with current import, mode, settled-event, event-cleanup, and transcript-visibility contracts.
- ADDED/UPDATED per-extension READMEs for the modified extension surfaces and hardened metadata discovery against nested =node_modules= trees.
- MOVED cross-extension and single-file extension tests from =pi/extensions/*.test.ts= to =pi/extensions/test/= so Pi does not auto-load them as extension factories; metadata validation and the ext-dev contract enforce the placement.

** Gotchas
- Pi auto-discovers every direct =~/.pi/agent/extensions/*.ts= or =.pi/extensions/*.ts= file as an extension factory; tests and support modules must be nested below the root and must not be named =index.ts= unless they are extension entry points.
- A stale project-local =node_modules/.bin/pi= can shadow installed pi 0.80.10 when RPC tests run through =npx=; integration harnesses isolate the agent directory and resolve the installed binary deliberately.
- Development dependencies under an extension can contain example extensions; metadata discovery must skip =node_modules= at every traversal depth.

** Risks
- Herdr runtime behavior is unchanged locally until follow-up [[task:a6be7668-26a2-4b12-b2b9-c40d07af55dc][a6be7668]] lands in the external canonical generator.

** Follow-ups
- [[task:a6be7668-26a2-4b12-b2b9-c40d07af55dc][Upstream Herdr settled-state Pi integration]] owns the external canonical generator change; the Herdr-managed local output remains untouched.

* Plan

** DONE [#A] Correct interactive mode and tool concurrency boundaries
CLOSED: [2026-07-20 Mon 15:55]
:PROPERTIES:
:CUSTOM_ID: bfa88000-74e5-4b5d-aae9-22f9317f254c
:CREATED: [2026-07-20 Mon 14:20]
:STARTED: [2026-07-20 Mon 15:25]
:END:
:LOGBOOK:
- State "STARTED" from "TODO" [2026-07-20 Mon 15:25]
- State "DONE" from "STARTED" [2026-07-20 Mon 15:55]
:END:

** DONE [#A] Make shared event subscriptions session-scoped
CLOSED: [2026-07-20 Mon 16:19]
:PROPERTIES:
:CUSTOM_ID: 34c60f15-f3a1-4673-a312-5d5e8f2ccd30
:CREATED: [2026-07-20 Mon 14:20]
:STARTED: [2026-07-20 Mon 15:56]
:END:
:LOGBOOK:
- State "STARTED" from "TODO" [2026-07-20 Mon 15:56]
- State "WAITING" from "STARTED" [2026-07-20 Mon 15:57]
- State "STARTED" from "WAITING" [2026-07-20 Mon 16:03]
- State "DONE" from "STARTED" [2026-07-20 Mon 16:08]
- State "STARTED" from "DONE" [2026-07-20 Mon 16:09]
- State "DONE" from "STARTED" [2026-07-20 Mon 16:19]
:END:

** DONE [#A] Separate human transcript entries from model context
CLOSED: [2026-07-20 Mon 16:35]
:PROPERTIES:
:CUSTOM_ID: 8bdd367c-16aa-4261-810f-256a8dca01b5
:CREATED: [2026-07-20 Mon 14:20]
:STARTED: [2026-07-20 Mon 16:20]
:END:
:LOGBOOK:
- State "STARTED" from "TODO" [2026-07-20 Mon 16:20]
- State "DONE" from "STARTED" [2026-07-20 Mon 16:35]
:END:

** DONE [#A] Adopt standard rename diffs and coherent LSP cache invalidation
CLOSED: [2026-07-20 Mon 16:48]
:PROPERTIES:
:CUSTOM_ID: 24e85559-64ba-42a1-9966-9829425f74ef
:CREATED: [2026-07-20 Mon 14:20]
:STARTED: [2026-07-20 Mon 16:36]
:END:
:LOGBOOK:
- State "STARTED" from "TODO" [2026-07-20 Mon 16:36]
- State "DONE" from "STARTED" [2026-07-20 Mon 16:48]
:END:

** DONE [#B] Normalize public imports and agent-directory paths
CLOSED: [2026-07-20 Mon 17:12]
:PROPERTIES:
:CUSTOM_ID: 31955d91-f107-48d0-b6e1-8af3817b9554
:CREATED: [2026-07-20 Mon 14:20]
:STARTED: [2026-07-20 Mon 16:49]
:END:
:LOGBOOK:
- State "STARTED" from "TODO" [2026-07-20 Mon 16:49]
- State "DONE" from "STARTED" [2026-07-20 Mon 17:12]
:END:

** DONE [#A] Harden and simplify the owned pi-clojure extension
CLOSED: [2026-07-20 Mon 17:25]
:PROPERTIES:
:CUSTOM_ID: dcb32da7-a212-4c7e-ba0f-0c175d8c55f5
:CREATED: [2026-07-20 Mon 15:25]
:STARTED: [2026-07-20 Mon 17:12]
:END:
:LOGBOOK:
- State "STARTED" from "TODO" [2026-07-20 Mon 17:12]
- State "DONE" from "STARTED" [2026-07-20 Mon 17:25]
:END:

** DONE [#B] Move Herdr state reporting to settled-agent semantics
CLOSED: [2026-07-20 Mon 17:30]
:PROPERTIES:
:CUSTOM_ID: ee397aba-290d-4b94-bc30-be2c3ff60ccc
:CREATED: [2026-07-20 Mon 14:20]
:STARTED: [2026-07-20 Mon 17:26]
:END:
:LOGBOOK:
- State "STARTED" from "TODO" [2026-07-20 Mon 17:26]
- State "DONE" from "STARTED" [2026-07-20 Mon 17:30]
:END:

** DONE [#B] Refresh extension contracts and run the complete validation matrix
CLOSED: [2026-07-20 Mon 17:42]
:PROPERTIES:
:CUSTOM_ID: c1c56136-e239-46ab-8fb5-6bc2f43866b7
:CREATED: [2026-07-20 Mon 14:20]
:BLOCKED-BY: task:bfa88000-74e5-4b5d-aae9-22f9317f254c
:BLOCKED-BY+: task:34c60f15-f3a1-4673-a312-5d5e8f2ccd30
:BLOCKED-BY+: task:8bdd367c-16aa-4261-810f-256a8dca01b5
:BLOCKED-BY+: task:24e85559-64ba-42a1-9966-9829425f74ef
:BLOCKED-BY+: task:31955d91-f107-48d0-b6e1-8af3817b9554
:BLOCKED-BY+: task:dcb32da7-a212-4c7e-ba0f-0c175d8c55f5
:BLOCKED-BY+: task:ee397aba-290d-4b94-bc30-be2c3ff60ccc
:STARTED: [2026-07-20 Mon 17:30]
:END:
:LOGBOOK:
- State "STARTED" from "TODO" [2026-07-20 Mon 17:30]
- State "DONE" from "STARTED" [2026-07-20 Mon 17:42]
:END:

** DONE [#A] Keep test modules out of extension auto-discovery roots
CLOSED: [2026-07-20 Mon 17:49]
:PROPERTIES:
:CUSTOM_ID: 120580a4-6e72-44e7-b9e5-278d66823cf3
:CREATED: [2026-07-20 Mon 17:49]
:STARTED: [2026-07-20 Mon 17:49]
:END:
:LOGBOOK:
- Created [2026-07-20 Mon 17:49]
- State "STARTED" from "TODO" [2026-07-20 Mon 17:49]
- State "DONE" from "STARTED" [2026-07-20 Mon 17:49]
:END:

* Implementation
- Interactive boundaries: =question= is sequential and TUI-only; bare =/tasks= is TUI-only while =/tasks new= and =/tasks doctor= remain RPC-capable. Unit, component-smoke, and RPC integration coverage enforce the distinction.
- Human-only prompt display: =/systemprompt= uses =appendEntry=/=registerEntryRenderer= in TUI and =ctx.ui.notify= in RPC. Its RPC harness isolates the agent directory and resolves the installed pi 0.80.10 binary rather than the stale project-local pi binary.
- LSP rename diffs: workspace edits capture before/after content under the existing per-file mutation queues, invalidate previews before document refresh, and bound both model content and expandable details. The pi 0.80.10 development dependency was pulled forward as the narrow prerequisite for the public diff helper; full scope normalization followed in the next task.
- Imports and paths: maintained extensions now use current package roots and =typebox= 1.1.38. A shared =getAgentPath()= derives global paths from =getAgentDir()= while the independent =~/.agents/skills= fallback remains literal; RPC harnesses isolate their agent directories.
- Owned Clojure tooling: nREPL timeout starts before connect, tool cancellation destroys the socket, all transport outcomes settle once, and each discovery probe has its own budget. Eval/repair content and details use pi's standard bounds; the production-only install was verified outside the repository with host-provided pi/TypeBox peers and local =bencode=/=parinfer= runtime dependencies.
- Herdr ownership: the canonical asset is external at =ogulcancelik/herdr:src/integration/assets/pi/herdr-agent-state.ts=, embedded by =src/integration/mod.rs=. Upstream integration v6 still carries the retry heuristic and listener leak, so local task [[task:a6be7668-26a2-4b12-b2b9-c40d07af55dc][a6be7668]] owns delivery; the generated local file was not edited.
- Final validation: the complete extension matrix, metadata/link checks, package-scope/path scans, and =ot doctor= pass. Metadata discovery now ignores nested dependency trees, so full =pi-clojure= development installs do not create false extension collisions.
- Test placement regression: root-level test modules were moved under =pi/extensions/test/=, runners and docs follow the new paths, metadata validation rejects recurrence, and a real pi 0.80.10 RPC startup completes without factory-load errors.
