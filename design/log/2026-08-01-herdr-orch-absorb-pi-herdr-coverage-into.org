#+TITLE: herdr-orch: absorb pi-herdr coverage into the bb CLI, rename skill and binary to oh
#+DATE: 2026-08-01 Sat
#+PARENT: [[task:4a9a24b3-2518-4cf5-9d8d-64b5fa0c76ba][herdr-orch: absorb pi-herdr coverage into the bb CLI, rename skill and binary to oh]]
#+SETUPFILE: ../../TASKS.setup.org
#+STATUS: Accepted
#+SPEC: [[proj:skills/herdr-subagents/SKILL.md]]
#+SPEC: [[proj:skills/herdr-subagents/scripts/docs/contract.md]]

* Intent

Own the Herdr control surface first-party instead of depending on a floating upstream pi extension, and rename the skill and CLI so the name matches what they actually do: orchestrate terminal topology *and* delegation.

* Summary

*Level:* Standard · *Tests:* extend the existing bb suite; new coverage per operation group · *Docs:* SKILL, contract, README, AGENTS subagent section

Replace =@ogulcancelik/pi-herdr= with coverage in the existing harness-agnostic =herdr.clj=, expose it through object-namespaced CLI verbs, keep tool discoverability via a thin first-party pi proxy that exposes window management only, then rename =herdr-subagents= → =herdr-orch= and =subagent= → =oh=.

** Scope

*** In scope

- Complete =herdr.clj= to the 27 operations pi-herdr exposes: 16 are missing (=workspace list/create/focus=, =tab list/focus=, =pane list/current/read/run/send-text/send-keys/wait-output=, =agent read/send-keys/focus/rename=). Note the current 11 are not a strict subset either way — =pane rename= exists here and not in pi-herdr.
- Port the *behaviours*, not just the plumbing: the self-close guard (refuse to close the pane the caller runs in), output truncation on read/wait-output, human-readable pane/agent list summaries, herdr error-envelope mapping, and lifecycle =until= semantics on agent wait.
- Object-namespaced CLI verbs: =oh pane|tab|ws|agent <op>= for mechanics, =oh task <op>= for delegation, plus =oh spawn "<command>"= as sugar for the unambiguous infrastructure case (create tab, run, report the pane).
- A thin first-party pi extension exposing =herdr_layout=/=herdr_pane=/=herdr_agent= as passthroughs to =oh=, with all logic remaining in Babashka.
- Remove =npm:@ogulcancelik/pi-herdr= from =pi/settings.json=.
- Rename: skill directory =herdr-subagents= → =herdr-orch=, launcher =subagent= → =oh=, env prefix =HERDR_SUBAGENT_*= → =HERDR_ORCH_*=, ledger/result directory =.agents/tmp/herdr-subagents/= → =.agents/tmp/herdr-orch/=, and every reference across docs, tests, personas and =AGENTS.md=.

*** Out of scope

- Backward compatibility. This is dev-only infrastructure with no external consumers, so no shim, alias or deprecation window is provided — in-flight children are drained instead (see =** Risks=).
- The rest of the herdr CLI beyond pi-herdr's surface: =worktree=, =session=, =notification=, =config=, =channel=, and =pane zoom|swap|move|resize|neighbor|edges|process-info=. Add on demand.
- =pi/extensions/herdr-agent-state.ts=. A different concern (reporting pi lifecycle *into* Herdr), regenerated by Herdr, and owned by task =a6be7668=.
- Renaming =.agents/subagents/= persona directories — see the decision below.
- Delegation as a discoverable tool. Deliberately excluded; see the decision below.

** Acceptance

*** Core functionality

- [ ] =herdr.clj= covers all 27 pi-herdr operations; a test enumerates the expected set so a future gap fails loudly
- [ ] Self-close guard: closing the caller's own pane is refused with a clear error, verified by test
- [ ] Read/wait-output truncate to the documented bound, and an empty =recent-unwrapped= read falls back to =visible= rather than returning nothing
- [ ] Pane and agent list output is human-readable one-line-per-entry, not raw JSON
- [ ] =oh pane|tab|ws|agent= and =oh task= verbs work; =oh task run= and =oh agent start= are visibly different operations in =--help=
- [ ] =oh spawn "<command>"= creates a tab, runs the command, and reports the pane id
- [ ] The pi proxy extension exposes window management only, and no delegation verb is reachable as a tool
- [ ] =npm:@ogulcancelik/pi-herdr= is absent from =pi/settings.json= and the proxy provides equivalent tool coverage
- [ ] No =HERDR_SUBAGENT_=, =herdr-subagents= or =subagent= reference survives outside =design/log/= and =TASKS.archive.org=
- [ ] =bb test= green; a live end-to-end delegation completes and publishes under the new names

*** Anti-criteria

- Must not: modify or regenerate =pi/extensions/herdr-agent-state.ts=
- Must not: rename =.agents/subagents/= or =~/.agents/subagents/=, or change the persona filename/frontmatter contract
- Must not: expose delegation (=oh task *=) through the proxy extension
- Must not: begin the rename while any child is in flight
- Must not: put logic in the pi extension that belongs in Babashka — the extension is a passthrough

** Decisions

- Absorb into the bb CLI rather than dropping to the raw herdr CLI or adding a third skill layer :: the raw CLI loses the enforced self-close guard, argument validation and tool discoverability, trading a three-line upstream defect for a permanent capability loss; a =herdr-wm= skill on top would add a fourth layer while being unable to fix a tool that throws. Absorbing is cheap because =herdr.clj= is already the harness-agnostic mechanical layer at 98 lines and already covers 11 of 27 operations.
- Object-namespaced verbs; do *not* unify delegation under =spawn= :: =spawn "bb nrepl-server"= succeeds when a process is running, while delegating succeeds only when a validated RESULT envelope exists with ledger entry, persona/model resolution, allow-list, depth bound and closure rules. Same verb, incompatible post-conditions. Worse, the single most damaging finding of the advisor evaluation was a worker hand-rolling =herdr agent start= instead of =subagent run advisor=, which defeated tier selection, hid $1.6883 from the ledger and orphaned a pane; a surface where =spawn --kind pi= and =spawn <persona>= differ by a flag turns that category error into a typo. Rejected: unifying under =spawn=; a mandatory disambiguating mode flag (a dispatcher with a required argument is worse than two verbs).
- =oh= as the binary, =herdr-orch= as the skill :: =herdr-wm= under-describes the load-bearing half — skill loading is driven by the =description:= frontmatter, so \"window manager\" would mis-route discovery away from the part owning the envelope contract and the ledger. =oh= is free on PATH with no alias and matches the local two-character convention (=bb=, =pi=, =rg=, =fd=, =gh=, =jj=). Rejected: =ho= (derogatory English slang, and this repository is public); =hd= (taken by hexdump); =hwm= (pairs with the under-describing skill name). Residual: a niche Unix shell is also called =oh=, which would surface immediately if ever installed.
- Keep =.agents/subagents/= unrenamed :: those directories name their *contents* — subagent personas — not the owning skill, and a persona is still a subagent persona. =~/.agents/subagents/= is also deliberately unmanaged and user-owned, so renaming it would silently orphan user files. Verified safe: =core/persona-directories= builds the two override tiers as skill-independent literals, and the packaged tier derives script-relative from =skill-directory=, so the rename relocates packaged personas with zero code change.
- Delegation stays off the tool surface :: needing to load the skill to learn the contract is a useful gate. A discoverable =oh task run= tool would let an agent delegate without reading the envelope, ledger and closure rules — the documented route to uncaptured children and orphaned panes.
- Supersede rather than wait on the upstream fix :: task =34683f68= reports the pi-herdr defect upstream. First-party coverage fixes it by construction (exit-code semantics in bb), so this work does not depend on upstream latency. The report is still worth sending as a courtesy; close =34683f68= as superseded-for-our-purposes once the proxy lands.

** Risks

- The rename touches a wire contract, not just names: =$HERDR_SUBAGENT_BIN= is embedded in composed prompt text (=cli.clj:136,140= instruct the child to run it), the injected env carries =HERDR_SUBAGENT_*=, and ledger entries hold absolute =result= and =persona-path= values. A child in flight across the rename keeps a launcher path that no longer exists and cannot publish, stranding its work. Mitigation: the rename is the last task and begins only after =oh task list= (or the pre-rename equivalent) shows nothing in flight.
- Measured rename surface: 127 =herdr-subagents=, 102 =HERDR_SUBAGENT_*=, 43 =subagent= and 16 =HERDR_SUBAGENT_BIN= references across 31, 11, 18 and 9 files. Mechanical but wide; the test suite and the no-surviving-reference acceptance check are the guard.
- Taking first-party ownership means Herdr CLI changes become our breakage rather than upstream's. Accepted: =herdr.clj= already carried that exposure for 11 operations, so the increment is modest and the alternative is a dependency that silently misreports success.
- A passthrough extension can drift from the CLI it wraps. Mitigation: keep it a literal passthrough with no logic, so drift can only be a missing action rather than divergent behaviour.

* Plan

** DONE Complete the herdr.clj operation surface and port pi-herdr's behaviours
CLOSED: [2026-08-01 Sat 00:45]
:PROPERTIES:
:CUSTOM_ID: a6b07a1f-1609-4761-87ed-9239b33817f0
:CREATED: [2026-08-01 Sat]
:STARTED: [2026-08-01 Sat 00:35]
:END:
:LOGBOOK:
- State "STARTED" from "TODO" [2026-08-01 Sat 00:35]
- State "DONE" from "STARTED" [2026-08-01 Sat 00:45]
:END:
Add the 16 missing operations and the behaviours that make them usable. Behaviours matter more than plumbing here: the self-close guard is real enforcement the CLI lacks, and the read-source fallback fixes a trap that made a successful command look like it never ran.
Acceptance criteria:
- All 27 operations reachable from =herdr.clj=, with a test enumerating the expected set
- Self-close guard refuses closing the caller's own pane, with a test
- Read and wait-output truncate to the documented bound; an empty =recent-unwrapped= result falls back to =visible=
- Pane/agent list rendering is one readable line per entry
- Herdr error envelopes map to actionable messages; no-output subcommands are treated as success on exit 0
- Must not: expect JSON from subcommands that emit none — the defect being replaced

** DONE Expose object-namespaced CLI verbs plus spawn sugar
CLOSED: [2026-08-01 Sat 00:59]
:PROPERTIES:
:CUSTOM_ID: 0d56a81f-3f01-4530-a1ad-a1d380707d57
:CREATED: [2026-08-01 Sat]
:BLOCKED-BY: task:a6b07a1f-1609-4761-87ed-9239b33817f0
:STARTED: [2026-08-01 Sat 00:45]
:END:
:LOGBOOK:
- State "STARTED" from "TODO" [2026-08-01 Sat 00:45]
- State "DONE" from "STARTED" [2026-08-01 Sat 00:59]
:END:
Add =pane|tab|ws|agent= verb groups and move the delegation verbs under =task=, keeping them lexically distinct from raw agent control. Add =spawn "<command>"= as sugar for create-tab-plus-run.
Acceptance criteria:
- =oh pane|tab|ws|agent <op>= and =oh task run|start|collect|status|publish|progress|prune= all work
- =--help= presents delegation and raw agent control as distinct groups
- =oh spawn "<command>"= creates a tab, runs the command, and reports the pane id
- Existing delegation behaviour is unchanged apart from the verb path
- Must not: accept a =spawn= invocation that could be read as delegation

** TODO Add the thin pi proxy extension and drop pi-herdr
:PROPERTIES:
:CUSTOM_ID: 0b30ff43-aa5d-45aa-b845-f3915eb84656
:CREATED: [2026-08-01 Sat]
:BLOCKED-BY: task:0d56a81f-3f01-4530-a1ad-a1d380707d57
:END:
A passthrough extension under =pi/extensions/= exposing =herdr_layout=, =herdr_pane= and =herdr_agent= mapped onto =oh=, then remove =npm:@ogulcancelik/pi-herdr= from =pi/settings.json=. Check for tool-name collision before removal if both are briefly registered.
Acceptance criteria:
- The three tools work through the proxy for every action they advertise
- No delegation verb is reachable as a tool
- =npm:@ogulcancelik/pi-herdr= removed from =pi/settings.json=
- The extension contains no logic beyond schema, dispatch and error surfacing
- Must not: touch =pi/extensions/herdr-agent-state.ts=

** TODO Rename skill, binary, env prefix and ledger directory
:PROPERTIES:
:CUSTOM_ID: 9fa14b6c-3a4d-45ab-b3bd-bb5567d4b662
:CREATED: [2026-08-01 Sat]
:BLOCKED-BY: task:0b30ff43-aa5d-45aa-b845-f3915eb84656
:END:
The riskiest step, deliberately last. =git mv= the skill directory to =herdr-orch=, rename the launcher to =oh=, switch the env prefix to =HERDR_ORCH_*=, move the ledger/result directory, and carry all mechanical fallout in this task: namespaces, docs, tests, persona bodies, =AGENTS.md=, and the =agents.nix= symlink wiring if it names the skill.

*Root-session only — this task must not be delegated.* A delegated child holds =$HERDR_SUBAGENT_BIN= pointing at the very launcher it would rename, so it would destroy its own publish path mid-assignment and strand its work with no envelope. "Nothing in flight" therefore includes the executor itself: the root session performs this rename directly.
Acceptance criteria:
- No =herdr-subagents=, =subagent= or =HERDR_SUBAGENT_= reference outside =design/log/= and =TASKS.archive.org=
- =.agents/subagents/= and =~/.agents/subagents/= untouched; persona resolution still finds project, home and packaged tiers
- =bb test= green
- Verified no child in flight before starting
- Must not: delegate this task — the child would rename its own launcher and lose its publish path
- Must not: leave a compatibility alias — out of scope by decision

** TODO Verify end to end and retire the superseded upstream report
:PROPERTIES:
:CUSTOM_ID: 6a6785ab-1080-42ab-8cc8-9e572fb923f6
:CREATED: [2026-08-01 Sat]
:BLOCKED-BY: task:9fa14b6c-3a4d-45ab-b3bd-bb5567d4b662
:END:
Exercise the whole surface live, then settle =34683f68=.
Acceptance criteria:
- A live delegation completes and publishes under the new names, with a validated envelope and correct ledger entry
- =oh spawn "bb nrepl-server"= behaves correctly, including surfacing the address-in-use failure rather than hiding it
- Proxy tools verified from a pi session with pi-herdr absent
- =34683f68= closed as superseded, or retained solely as an upstream courtesy report with that scope noted
- =ot doctor= clean

* Implementation
